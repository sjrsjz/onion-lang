//f := mut () -> undefined;
@required stdlib;
f := mut stdlib.math.floor;
a := mut 100;
while (a > 0) {
    b := mut 100;
    while (b > 0) {
        c := mut 100;
        while (c > 0) {
            f(4);
            c = c - 1;
        };
        b = b - 1;
    };
    a = a - 1;
};