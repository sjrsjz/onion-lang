@required 'stdlib';
@include "../../std/marcos.onion";

module := () -> dyn { // dyn specifies that the lambda is constructed using dynamically generated bytecode rather than being determined at compile time
    import "./module.onionc" // Import the compiled module's bytecode
};
module := module(); // Instantiate the module(execute the lambda)
stdlib.io.println(@strcat("Imported module version:", $module.get_version()));